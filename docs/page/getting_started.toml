[data]
    # base template to use
    template = 'template:default'
    # title is not necessary, if none added it will use the title set in metadata.toml. you can tell it to use the file name as title
    # example : title 'getting_started' will show up as 'Getting Started'
    title = '$default_title $file_name'